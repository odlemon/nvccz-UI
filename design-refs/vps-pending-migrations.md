# VPS pending migrations (local → Contabo)

When Contabo (`arcus_dev` on the VPS) is reachable again, apply every row where `appliedToVps` is `false`.

**Rules**
- Every schema/data migration we run locally must be added here the same day.
- `appliedToLocal=true` once verified on local MySQL.
- `appliedToVps=false` until you explicitly ask to apply on the VPS.
- Do not invent Contabo changes outside this list.

| id | description | how to apply | appliedToLocal | appliedToVps | notes |
|---|---|---|---|---|---|
| `2026-08-26-application-form-data` | Add `applications.application_form_data` JSON NULL | `ALTER TABLE applications ADD COLUMN application_form_data JSON NULL;` (or `prisma db push` / migrate) | `true` | `false` | Added for funding form UoF/declarations/Impact-ESG persistence |
| `2026-08-27-local-mysql-baseline` | Full schema sync from `prisma/schema.prisma` on empty local DB | Local only: `npx prisma db push` | `true` | `false` | Not a Contabo delta; VPS already had schema. Skip on VPS unless rebuilding |
| `2026-08-27-assert-dev-db-arcus_dev` | Allow seeds/UAT on `arcus_dev` as well as `nts` | Deploy updated `scripts/lib/assert-dev-database.ts` | `true` | `false` | Code change, not SQL |
| `2026-08-27-portfolio-v11-local-demo-seed` | Seed admin@nts.com + PE fund + NTS/Demo ACTIVE_DD deals | `npx ts-node --transpile-only -r dotenv/config scripts/seed-portfolio-v11-local-demo.ts` | `true` | `false` | **Local-only demo recreate** — Contabo dump unavailable. Do **not** blindly re-run on VPS if production data exists |
| `2026-09-06-fund-vintage-currency-geography` | Add `funds.vintage_year` (Int?), `funds.currency_code` (VarChar(8)?), `funds.geography` (VarChar(191)?) | `npx prisma db push` (schema.prisma model `Fund`) | `true` | `true` | Portfolio Funds detail — Fund Terms card had no real column to persist Vintage/Currency/Primary Geography; frontend Create/Edit Fund forms now send these. Applied to arcus-dev-mysql-1 (arcus_dev), arcus-demo-mysql-1 (arcus_prod), nvccz-prod-mysql-1 (nvccz_prod) via raw SQL over SSH on 2026-09-06 (columns are nullable/additive — verified before and after on each). Container images still need a code redeploy to pick up the corresponding Prisma client/service changes. |
| `2026-09-06-fund-performance-snapshots` | New table `fund_performance_snapshots` (id, fund_id, as_of_date, total_commitment, called_capital, distributed_capital, nav, gross_irr, net_irr, tvpi, dpi, created_by_id, created_at) + unique(fund_id, as_of_date) + FK to `funds.id` (cascade) | `npx prisma db push` (schema.prisma model `FundPerformanceSnapshot`) | `true` | `true` | Backs the Funds detail NAV/J-Curve charts. `FundService.recordPerformanceSnapshot()` is called on fund create/update — no historical backfill exists (none to backfill from), series builds forward. One snapshot was seeded per existing local fund by triggering a no-op update via `PUT /funds/:id`. Applied to the same three VPS databases as the row above, same date/method. |
| `2026-09-06-fund-documents` | New table `fund_documents` (id, fund_id, name, category, file_url, storage_path, mime_type, file_size_bytes, uploaded_by_id, created_at, updated_at) + index(fund_id) | `npx prisma db push` (schema.prisma model `FundDocument`) | `true` | `true` | Portfolio Funds detail — Documents tab had 100% hardcoded fake document list with no upload capability. New `FirebaseStorageService.uploadFundDocument`, `POST/GET /funds/:fundId/documents` (multer, 50MB limit), frontend upload input + real preview/download wired via existing `showDocumentPreview`/`normalizeMediaUrl` infra. Verified live: real PDF upload → real render in viewer. Applied to the same three VPS databases, same date/method (no FK constraint — matches local, which Prisma db push did not add one for either). |
| `2026-09-07-role-portfolio-settings-permissions` | Add `roles.portfolio_settings_permissions` (JSON NULL) | `ALTER TABLE roles ADD COLUMN portfolio_settings_permissions JSON NULL;` (matches schema.prisma `Role.portfolioSettingsPermissions Json?`) | `true` | `true` | Undocumented drift found while running the role sync below — column existed on local `arcus_dev` (schema.prisma already has it) but was missing on both reachable VPS databases, blocking `prisma.role.findMany()`. Nullable/additive, applied via raw SQL over SSH tunnel to `arcus-dev-mysql-1` (`arcus_dev`, tunnel :3307) and `nvccz-prod-mysql-1` (`nvccz_prod`, tunnel :3327) on 2026-09-07. Did not check/touch `arcus-demo-mysql-1` (`arcus_prod`) — not in scope for this task, flag separately if this is needed there too. |
| `2026-09-07-sync-hardcoded-roles-vps` | Data-only: populate `roles` table from `src/config/hardcodedRoles.ts` (accounting-v52/admin-management audit found this had never been run beyond a handful of manually-created roles) | `npm run sync:roles` (`scripts/sync-hardcoded-roles.ts` — creates/updates by name, skips admin roles, idempotent) | `true` | `true` | Ran against local `arcus_dev` earlier the same session (6→59 roles), then VPS `arcus_dev` (6→58 roles) and VPS `nvccz_prod` (2→57 roles) via the SSH tunnels above on 2026-09-07. Required the column migration directly above first. Not run against `arcus_prod` (demo) — same scope note as above. |
| `2026-09-07-role-portfolio-settings-permissions-demo` | Add `roles.portfolio_settings_permissions` (JSON NULL) to the **Arcus Demo** database (`arcus-demo-mysql-1` / `arcus_prod`) | `ALTER TABLE roles ADD COLUMN portfolio_settings_permissions JSON NULL;` | `true` | `true` | Completes the row above, which covered only `arcus_dev` and `nvccz_prod`. Same nullable/additive column; verified PRESENT after. Applied 2026-09-07. |
| `2026-09-07-seed-departments-roles-demo` | Data-only: roles + departments on **Arcus Demo** (`arcus_prod`) | `docker exec arcus-demo-api-1 npx ts-node --transpile-only scripts/sync-hardcoded-roles.ts` and `scripts/01-seed-departments.ts` | `true` | `true` | Demo DB had 1 role and 0 departments. After: 56 roles, 9 departments. Both scripts are create-only/idempotent. Required the column migration above first. Applied 2026-09-07. |
| `2026-09-07-seed-chart-of-accounts-nvccz-prod` | Data-only: standard chart of accounts on **NVCCZ Production** (`nvccz-prod-mysql-1` / `nvccz_prod`) | `docker exec nvccz-prod-api-1 npx ts-node --transpile-only scripts/ensure-standard-chart-of-accounts.ts` then `scripts/ensure-capital-call-chart-accounts.ts` | `true` | `true` | **`chart_of_accounts` was completely empty (0 rows) on the live client production database**, so the accounting module had no GL accounts to post against. Created 32 standard accounts, 0 errors; capital-call accounts 1210/3010 confirmed present. Script is create-if-missing, never overwrites. Applied 2026-09-07. |
| `2026-09-07-chart-of-accounts-demo-verify` | Verify standard chart of accounts on **Arcus Demo** | same `ensure-standard-chart-of-accounts.ts` | `true` | `true` | Ran to fill gaps; result `Created: 0, Already present: 32` — demo already complete (33 rows incl. one extra local account). Applied 2026-09-07. |
| `2026-09-07-arcus-prod-mysql-unreachable` | **Not applied — blocked.** `arcus-prod-mysql-1` (`arcus_prod`, the `arcus.co.zw` MySQL-only instance) | n/a | `n/a` | `false` | Container is in a restart/crash loop: `[InnoDB] Unable to lock ./ibdata1 error: 11` (another mysqld holds the data files). Pre-existing infrastructure fault, unrelated to these changes. No roles/departments/CoA sync could be applied. Needs the volume/lock conflict resolved before it can be seeded. Flagged 2026-09-07. |
| `2026-09-07-board-review-votes-agreement-links` | `board_review_votes`, `fundraising_agreement_templates`, `fundraising_agreements.application_id/fund_id` (+indexes), `fundraising_agreement_signatories.role` | `npm run db:migrate:board-review-votes-agreement-links` (`scripts/run-board-review-votes-and-agreement-links-migration.ts`) | `true` | `true` | These were created implicitly by the `prisma db push` that used to run in the API entrypoint. That call was removed (it wanted to DROP live raw-SQL tables), so they needed an explicit migration. **All three server DBs were missing every object** — applied to arcus_dev, nvccz_prod and arcus_prod (demo) on 2026-09-07. |
| `2026-09-07-portfolio-fund-schema` | `funds.vintage_year/currency_code/geography`, `fund_performance_snapshots`, `fund_documents` | `npm run db:migrate:portfolio-fund-schema` | `true` | `true` | Replaces the manual raw SQL used earlier in the session, so a fresh environment can create these now that `db push` no longer runs. Idempotent; verified `[skip]` on all objects locally. |
| `2026-09-07-financial-reports-table-prod-demo` | `financial_reports` (+ `reporting_notification_logs` where the SQL creates it) | `mysql < scripts/sql/add_financial_reports_table.sql`, then `db:migrate:financial-reports-report-type-varchar` and `db:migrate:financial-reports-reporting-period` | `true` | `true` | **`financial_reports` did not exist on nvccz_prod at all**, despite 18 source files referencing it (FinancialReportService, ApplicantPortalController / investee portal). It is deliberately managed outside Prisma, so `db push` never created it either — it was only ever applied by hand on dev. Found because `db:migrate:all` reported `[FAILED]` on the two dependent migrations. Applied to nvccz_prod and arcus_prod (demo) 2026-09-07; demo still lacks `reporting_notification_logs` (created by a different source — follow up). |
| `2026-09-07-db-migrate-all-prod` | Full `db:migrate:all` sweep on nvccz_prod | `docker exec nvccz-prod-api-1 npm run db:migrate:all` | `n/a` | partial | Two earlier attempts died when the SSH session dropped (exit 255) — must be run detached (`setsid nohup … & disown`). Several `:all-dbs` variants fail inside a single-DB container, which appears expected. Re-run and review `[FAILED]` lines before trusting a clean bill of health. |
| `2026-09-07-recurring-journal-tables-verify` | Verify `recurring_journal_templates` / `recurring_journal_runs` on local `arcus_dev` | `npm run db:migrate:recurring-journal-tables` (pre-existing script, was already written but apparently never run/verified anywhere — no HTTP route existed to use the tables it creates) | `true` | `false` | Ran while building the accounting-v52 Recurring Schedules page's live wiring: both tables already existed locally (`[skip] ... exists` for each — idempotent, no-op). Added the missing HTTP layer (`RecurringJournalController`, `recurringJournalRoutes.ts`, mounted at `/api/accounting/recurring-journal-templates`) wrapping the pre-existing, fully-working `RecurringJournalService`. Verified end-to-end locally: created a template, ran it, confirmed a real POSTED journal entry was created. Not yet checked on any VPS database — run the same idempotent script there before relying on this feature outside local dev. |
| `2026-09-07-accounting-documents` | New table `accounting_documents` (id, name, category, file_url, storage_path, mime_type, file_size_bytes, uploaded_by_id, created_at, updated_at) | `npm run db:migrate:accounting-documents` (`scripts/run-accounting-documents-migration.ts`) | `true` | `false` | accounting-v52 Document Vault — sibling of `fund_documents`. New `FirebaseStorageService.uploadAccountingDocument()`, `/api/accounting/documents` routes. Verified live: real file upload → real storagePath/fileUrl → real render. |
| `2026-09-07-tax-return-pack-gaps` | Add `tax_return_packs.filing_due_date` (DATE NULL) | `npm run db:migrate:tax-return-pack-gaps` (`scripts/run-tax-return-pack-gaps-migration.ts`) | `true` | `false` | accounting-v52 Compliance & Tax page needed a real due-date field; pre-existing `TaxReturnPack` model had none. |
| `2026-09-07-accounting-approval-configs-seed` | Data-only: seed `ProcurementApprovalConfig`/`ProcurementApprovalStage` for `stageType:'MASTER_DATA_COA'`, approver `roleCode:'CFO'` | `scripts/run-accounting-approval-configs-seed.ts` (idempotent) | `true` | `false` | Enables Chart of Accounts edits by non-privileged users to route through the real Approval Centre instead of applying directly. Also fixed a latent bug in `ApprovalService.createApprovalRequest`'s config lookup (now requires `stages:{some:{stageType}}`) that would otherwise break routing once a second config exists. |
| `2026-09-07-accounting-close-tasks` | New table `accounting_close_tasks` (id, fiscal_period_id, workstream, task, owner_id?, due_at?, status, depends_on_id? self-FK, completed_by_id?, completed_at, created_at, updated_at) | `npm run db:migrate:accounting-close-tasks` (`scripts/run-accounting-close-tasks-migration.ts`) | `true` | `false` | accounting-v52 Period Close checklist — real per-fiscal-period task tracking, no prior model existed. Wired to the existing (already-real) `PeriodLockService`/fiscal-calendar lock-commit flow. |
| `2026-09-07-timesheets-projects` | New tables `projects`, `timesheets`, `timesheet_entries` (header+lines; `timesheets` unique on `(user_id, week_ending)`) | `npm run db:migrate:timesheets-projects` (`scripts/run-timesheets-projects-migration.ts`) | `true` | `false` | Genuinely new domain — no Timesheet/Project model existed anywhere. Routes at `/api/accounting/projects`, `/api/accounting/timesheets/*` (mine, pending-approval, entries, submit/approve/return). Approve/return write path + Command-tab weekly grid live-verified end-to-end same day (see below). |
| `2026-09-07-journal-entry-forecast-entity` | Add `journal_entries.forecast_entity_id` (nullable FK → `forecast_entities`, `onDelete:SetNull`) + `journal_entries.is_elimination_entry` (Boolean default false) | `npm run db:migrate:journal-entry-forecast-entity` (`scripts/run-journal-entry-forecast-entity-migration.ts`) | `true` | `false` | accounting-v52 Group Consolidation — backfilled all pre-existing journal entries onto a newly-created default `ForecastEntity` "Matanho Capital Partners" (`id: cmtr97bwc0001unnkqaslbfq3`, `isDefault:true`). `ConsolidatedReportService.getConsolidationSummary()` extended for multi-entity + eliminations. Single real entity today, so consolidated/standalone views legitimately show the same numbers — honest, not a bug. |
| `2026-09-07-accounting-fiscal-calendar-seed` | Data-only: seed `FiscalYear` "FY2026" + 12 monthly `FiscalPeriod` rows, all `status:'OPEN'` | Ad-hoc script, run once and deleted (not committed) | `true` | `false` | Zero `FiscalYear`/`FiscalPeriod` rows existed anywhere — Period Close had no current period to resolve against. Deliberately left all 12 periods OPEN (no real close history exists to back a CLOSED claim). Re-run an equivalent seed on any other environment before relying on Period Close there. |
| `2026-09-07-accounting-period-lock-permissions` | Data-only: add `accounting.period_lock.view\|manage\|audit_view\|override` to the `admin` role's permissions JSON | Direct role-permissions update (not scripted/committed) | `true` | `false` | `admin` role had zero `accounting.period_lock.*` permissions despite holding broad `manage_accounting` — blocked all Period Close API calls with 403. Confirmed the permission check is live against the DB (took effect without re-login). Needs the equivalent grant on any other environment's `admin` role. |
| `2026-09-07-cfo-role-test-assignment` | Data-only: set `roleCode:'CFO'` on test user `audit-test-admin-01` | Direct user update (not scripted/committed) | `true` | `false` | Zero users held the CFO role anywhere, and `ApprovalService.ts` resolves `stage.roleCode` against `User.roleCode` directly — Master Data / Payment / Investment approvals had no real user who could ever approve them. A real (non-test) CFO-role assignment is still needed on any environment where these approval types should actually function. |
| `2026-09-07-tax-return-pack-vat-fields` | Add `tax_return_packs.vat_output_tax`/`vat_input_tax`/`vat_net_payable` (Decimal NULL) | `npm run db:migrate:tax-return-pack-vat-fields` (`scripts/run-tax-return-pack-vat-fields-migration.ts`) | `true` | `false` | Extends the tax-return-pack compile step to real regime branching: VAT reuses `VATReportService`'s real GL aggregation (no GL accrual double-post — VAT is already reflected via normal invoice/expense postings); WHT is evidence-only (honestly found 0 withholding-tax GL accounts exist locally, writes that finding rather than a guessed liability); SAF-T is a readiness-count snapshot only, no file generated. Frontend "New compliance pack" flow (create+compile in one action) live-verified end-to-end for VAT and WHT the same day. |
| `2026-09-07-payment-investment-approval-configs-seed` | Data-only: seed `ProcurementApprovalConfig`/`ProcurementApprovalStage` for `stageType:'PAYMENT_EXECUTION'` ($10k USD-equiv threshold) and `'INVESTMENT_BOOKING'` ($50k USD-equiv threshold), approver `roleCode:'CFO'` | `npm run db:migrate:payment-investment-approval-configs-seed` (`scripts/run-payment-investment-approval-configs-seed.ts`, idempotent) | `true` | `false` | Gates `PurchaseInvoiceService.payInvoice`/`.payInvoicesBatch` and `ShortTermInvestmentService.createInstrument` — non-privileged users at/above threshold now get a real pending approval instead of executing immediately (privileged CFO/admin users and below-threshold amounts execute immediately, unchanged). Thresholds are a documented reasonable default, not an existing product policy — reconsider before relying on them for real payment controls. Live-verified end-to-end (seeded a real request via `ApprovalService.createApprovalRequest`, confirmed it renders correctly — real $15,000.00 amount, real maker, real required-authority label — in the Approval Centre). |

| `2026-09-08-performance-test-users` | Data-only: five real users, one per Performance-module audience tier — `perf.sysadmin@nts.local` (SYSADMIN), `perf.exec@nts.local` (CEO), `perf.hr@nts.local` (HR_MGR, Human Resources/HEAD), `perf.deptmgr@nts.local` (OPS_MGR, Operations/HEAD), `perf.employee@nts.local` (OPS_MEM, Operations/MEMBER) | `npm run db:seed:performance-test-users` (`scripts/seed-performance-test-users.ts`, idempotent; `-- --remove` deletes them) | `true` | `false` | **Test-only — deliberately NOT for VPS.** The Performance V22 UI gates everything on a client-side "demo role" toggle whose five roles (SysAdmin / Executive / HR-M&E Manager / Department Manager / Employee) match no real role held by any real user, making role behaviour unverifiable (see `performance-module-map.md` §7, §9.2). Each seeded roleCode exists in the frontend `RoleCode` union, already carries a `performance-management` grant in `ROLE_PERMISSIONS_MAP`, and maps to an existing `roles` row. There is no manager→report relation on `User` (`Employee.lineManagerUserId` exists but that table is empty), so the Department Manager / Employee pair is distinguished by `departmentRole` HEAD vs MEMBER within the same department. All five verified to authenticate against `POST /api/auth/login` (200, correct roleCode + role name). Password `admin123`, matching the other local test logins. |

| `2026-09-08-performance-role-permissions` | Data-only: grant `accounting.timesheets.view` to all 52 internal staff roles and `accounting.timesheets.manage` to the 16 manager/exec roles | `npx ts-node --transpile-only -r dotenv/config scripts/run-performance-role-permissions-migration.ts` (idempotent; `--check` reports without writing) | `true` | `false` | **`hardcodedRoles.ts` defines roles as `{name, level, department, description}` with no permissions field at all**, so `sync-hardcoded-roles.ts` created 57 roles with an empty `permissions` array — only `admin` (34 perms) and one stray local test role had any. Every route guarded by `requireAnyPermission(...)` therefore 403'd for all 57 real roles. Verified on `GET /api/accounting/timesheets/mine`, which the Performance Timesheets page depends on: admin 200, but SYSADMIN / CEO / HR_MGR / OPS_MGR / OPS_MEM all **403** — including the employee whose own timesheet it is, so self-service timesheets had never worked for anyone. After: all five staff roles 200 on `/mine`; `perf.employee` still correctly **403** on the manager-only `/pending-approval`; external `lp.test` still correctly **403** on `/mine`. Deliberately narrow — it grants only the keys the Performance module needs. **The wider gap is NOT fixed: the granular permission system still has no data behind it for any role except `admin`, which affects other modules too.** |


| `2026-09-08-performance-corrective-actions` | New table `performance_corrective_actions` (id, reference UNIQUE, title, description, `trigger_source`, owner_id, department_id, severity, status, progress, target_date, closed_at, created_by_id, created_at, updated_at) + 5 indexes + FKs to `users`/`departments` | `npm run db:migrate:performance-corrective-actions` (`scripts/run-performance-corrective-actions-migration.ts`, idempotent) | `true` | `false` | The Corrective Actions page had **no model at all** — it rendered five invented actions (CA-2026-014 … -027) with invented owners, triggers, progress bars and target dates, plus five KPI cards ("Open actions 68 +8% vs last month") asserted against nothing. Column is `trigger_source` because `TRIGGER` is a MySQL reserved word; Prisma maps it via `@map`. `reference` (CA-001, CA-002 …) is allocated server-side by `CorrectiveActionService`, so two concurrent creates cannot collide. Round-tripped against the live API: create → read → update → list → delete → 404. |

| `2026-09-08-performance-alerts` | New table `performance_alerts` (id, reference UNIQUE, title, description, source, severity, status, owner_id, department_id, escalation_level, raised_at, acknowledged_at, resolved_at, created_by_id, created_at, updated_at) + 6 indexes + FKs to `users`/`departments` | `npm run db:migrate:performance-alerts` (`scripts/run-performance-alerts-migration.ts`, idempotent) | `true` | `false` | The Alerts & Audit page had no model. Deliberately **no `elapsed` column**: elapsed time is derived from `raised_at` on every read (`elapsedMinutes`) so it can never go stale. Lifecycle verified live: create → acknowledge → escalate (level 1) → resolve → delete, plus `/summary` tallies. Note the first version of the script created `created_by_id` only on the CREATE TABLE path, so a pre-existing table would never have gained it — an `ensureColumn` and its index were added afterwards. |

| `2026-09-08-performance-documents` | New table `performance_documents` (id, name, folder, category, version, status, file_url, storage_path, mime_type, file_size_bytes, owner_id, department_id, retention_until, uploaded_by_id, created_at, updated_at) + 5 indexes + FKs to `users`/`departments` | `npm run db:migrate:performance-documents` (`scripts/run-performance-documents-migration.ts`, idempotent) | `true` | `false` | The Document Vault had no model — the folder bar, version/status lifecycle and retention column were all presentation over nothing. Company-wide store keyed by a free-string `folder` (Contracts / Reviews / Evidence / Policies), same shape as `AccountingDocument` plus owner / department / retention. |

| `2026-09-08-performance-register-permissions` | Data-only: grant `performance.corrective.view`, `performance.alerts.view`, `performance.documents.view` to all 52 internal staff roles, and the matching `.manage` keys to the 16 manager/exec roles | `npm run db:migrate:performance-role-permissions` (same idempotent script as `2026-09-08-performance-role-permissions`, extended; `--check` reports without writing) | `true` | `false` | The three new register route files shipped `authenticate`-only, meaning **any logged-in user of any portal — an LP, an applicant, an investee — could list, create, edit and delete performance records**. `requireAnyPermission` guards were added to all 21 routes across the three files (read = `.view` or `.manage`, write = `.manage`). Verified per role over HTTP: no token → 401 on all three; `perf.employee` (OPS_MEM) → 200 on reads, **403** on POST; `perf.deptmgr`/`perf.hr`/`perf.exec`/`perf.sysadmin` → 201 on POST. External roles (applicant, INVESTEE, Limited Partner, LP Viewer, LP Institutional Manager, External Auditor) hold **none** of the six keys and are therefore refused on read as well as write. |


| `2026-09-08-performance-settings` | New table `performance_settings` (id, `setting_key` UNIQUE, `value` JSON, `updated_by_id`, `created_at`, `updated_at`) + FK to `users`; data-only grant of `performance.settings.view`/`.manage` to the same 52/16-role split as the other three registers | `npm run db:migrate:performance-settings` then `npm run db:migrate:performance-role-permissions` (both idempotent) | `true` | `false` | The Settings page rendered a complete configuration workspace — org name, timezone, cascade, performance cycle, KPI/review approval levels, five notification cadences, four financial-year dates, four security switches — with **no table at all**; "Save changes" was wired to `toast-generic` (success toast, no request), and the RBAC panel's "Save RBAC policy" wrote to `localStorage` only. Keyed store (`namespace.name` -> JSON value) rather than a wide table, because no written spec for the field list exists — it was read off the rendered form, not a requirements doc. Deliberately seeds nothing: an unset key must read as unset, never as a default someone might mistake for a choice. Verified over HTTP: no token 401, employee GET 200/PUT 403, HR Manager PUT 200 (3 keys, one transaction) -> GET reflects all three with per-key `updatedBy`/`updatedAt` provenance, invalid-namespace key 400, DELETE returns a key to absent (not to a default). |


| `2026-09-08-performance-endpoint-guards-pass2` | Data-only: grant 15 new `performance.<domain>.view`/`.manage` key pairs (goals, scorecards, bsc, reviews, contracts, config, kpi, integration, etl, workflow, dashboard, analytics, rollup, risks, breakdown) — same 52-internal-role / 16-manager-role split as the pass-1 registers | `npm run db:migrate:performance-role-permissions` (idempotent, same script extended) — paired with a code change (not a DB migration): `src/middleware/methodGuard.ts` (new) applied to 22 route files, 118 write endpoints + their reads | `true` | `false` | Second and larger unguarded-endpoint finding: every performance-module route file beyond the pass-1 registers was `authenticate`-only — 118 write endpoints (create/update/delete goals, scorecards, BSC entries, reviews, review cycles, contracts, strategy config, KPIs, integration sync, ETL runs, workflow transitions) reachable by any logged-in user of any portal, plus their reads. Guarded via one router-wide `methodGuard()` per file (GET/HEAD -> `.view`, everything else -> `.manage`) rather than per-route edits, so a route added later inherits the guard instead of shipping unguarded by omission — the exact way this happened the first time. **Two routes deliberately excluded after checking consumers**: `board-reviews` backs the Portfolio/Investment-Committee deal flow (verified via `lib/api/board-review-api.ts`, `lib/portfolio-v11/*` — guarding it would have locked out deal analysts holding no Performance permission); `risk-assessments` WAS included (domain `risks`) after confirming no other backend service touches `prisma.riskAssessment` and no other frontend module calls the route, despite the model's deal-DD-shaped schema. Also checked `goal-api.ts`/`task-api.ts`, which hit the same `/performance/goals` and `/performance/individual-goals/*` paths from the orphaned legacy `components/performance/` module (no live route renders it) and from Employee Hub, whose users are internal staff and already hold the granted `.view` keys. Verified over HTTP: no token 401 across all 15 domains; employee 200 on every guarded read and **403** on writes to goals/scorecards; HR Manager write reaches the controller (400 validation error, not 403); external roles (`applicant`/`INVESTEE`/`Limited Partner`) confirmed at the DB layer to hold none of the new keys. |


| `2026-09-08-scorecard-pillars-seed` | Data-only: seed the 4 canonical BSC pillars (Financial / Customer & Market / Internal Operations / Learning, Growth & HR) into `scorecard_pillars`, weight 25% each | `npm run db:migrate:scorecard-pillars` (`scripts/run-scorecard-pillars-migration.ts` — pre-existing, idempotent, simply never run) | `true` | `false` | **The entire Performance scoring engine was blocked by one empty table.** `PerformanceScorecardService` (goal scoring, department/employee/CEO/board scorecards, the org-wide BSC dashboard) is fully built and real — not a stub — but every `POST /api/performance/goals` call with a `scorecardPillar` failed validation ("Invalid scorecardPillar…") because `ScorecardPillarService.assertCanonicalExists()` resolves against the `scorecard_pillars` table, which had 0 rows. No goal could ever be created, so nothing could ever be scored, so every scorecard/heatmap/trend across the module was correctly showing an honest dash — not because scoring wasn't built, but because its one prerequisite table was never seeded. Verified end to end after running the migration: created a real `PerformanceContract`, linked a real `PerformanceGoal` (92% progress) to it, and called `GET /performance/contracts/scorecards/user` — got back a real computed `weightedScore`, `progressPct: 80`, `rawRating`, not a fixture. Probe contract/goal deleted afterward. |

| `2026-09-08-performance-goal-write-gaps` | No schema change — two code-only bugs in `nvccz` fixed together, both on the write path for `POST /performance/goals` | n/a (code fix, no migration script) | `n/a` | `n/a` | (1) `PerformanceGoalController.createPerformanceGoal` never destructured `kpiId`/`scorecardSection` from `req.body`, even though `PerformanceGoalService`'s `CreatePerformanceGoalRequest` interface already declared both — silently dropping them on every request, so no client-created company goal could ever carry a real KPI and no governance indicator (Board/CEO scorecard section A1–D) could ever be created via the API at all, only via a seed script. (2) Deeper bug found after fixing (1): even with `scorecardSection` reaching the service, `PerformanceGoalService.createPerformanceGoal`'s `prisma.performanceGoal.create({data:{...}})` call never included `scorecardSection` in the write — every goal was silently persisted with `scorecardSection: null` regardless of what was sent, which is why the Board/CEO scorecard's indicator query (`where:{performanceContractId}`, grouped by `scorecardSection`) always returned `indicators: []` even for goals correctly linked to a Board contract. Both fixed in `src/controllers/PerformanceGoalController.ts` and `src/services/PerformanceGoalService.ts`. Verified live: (a) a `type:"company"` goal with a real `kpiId` made `GET /performance/scorecards/org-bsc` return a real non-zero `orgBscScore` for the first time; (b) five goals with `scorecardSection` A1–D linked to the real Board contract (`cmtt46pzs0006unuc10yueygs`) made `GET /performance/scorecards/board` return a real `finalScore: 3 ("Satisfactory")` across all five sections instead of the permanent "No governance indicators are linked" warning at score 0. CEO scorecard reads via the identical `performanceContractId`-only query shape, confirmed by code inspection (not yet separately probed with a live CEO contract). Probe goals deleted afterward; the Board contract itself was kept (real, reusable, not test debris — 2026 period, 0 lingering test goals). Runs on every environment automatically once deployed — no data migration needed. |

| `2026-09-08-performance-scorecard-entries` | New table `performance_scorecard_entries` (id, employee_id, period, kind [note\|evidence\|history\|publish], note_type, text, file_url, storage_path, mime_type, file_size_bytes, actor_id, created_at) + 3 indexes + FKs to `users` | `npm run db:migrate:performance-scorecard-entries` (`scripts/run-performance-scorecard-entries-migration.ts`, idempotent) | `true` | `true` | The Employee Scorecard page's supporting apparatus — review notes, evidence uploads, update history, publish workflow (`V.notes`/`V.evidence`/`V.history`/`V.products` in the vendored runtime) — was 100% `localStorage`-only and never touched a real API, despite the scorecard's own KPI/goal rows already being real (wired in an earlier session). One append-only event log per (employeeId, period) discriminated by `kind`, rather than four separate tables — mirrors how the UI already treats these as one activity stream. Evidence upload reuses the same `remoteUploadService` pipeline as `PerformanceDocumentService`. Publishing is an event, not a flag: "is published" = a `publish` entry exists with no later unpublish, read via `getPublicationStatus()`. New `PerformanceScorecardEntryService`, `PerformanceScorecardEntryController`, routes at `/api/performance/scorecard-entries` (list, notes, evidence, history, publish, delete), reusing the existing `performance.scorecards.view`/`.manage` permission keys rather than adding a new domain. Verified live end to end: created a note, a history entry and a publish event via real HTTP calls (all 201, transactional — each note/evidence/publish auto-appends its own history entry in the same `$transaction`), listed them back grouped by kind with a real `publication.published:true`/`publishedAt`/`publishedBy`, then deleted all three. Frontend wired the same day: `scripts/perf-patches/employee-scorecard-apparatus.mjs` replaces the runtime's `addEvidence()`/`saveNote()`/`publish()` (v18 employee-scorecard module) to call new `window.__PERF_SUBMIT_SCORECARD_{NOTE,EVIDENCE,PUBLISH}__` hooks (`lib/performance-v22-mock/actions.ts`) instead of writing to `V.notes`/`V.evidence`/`V.products`, and `scorecardPage()`'s read side now sources `hist`/`notes`/`evidence`/`product` from a live keyed cache (`bridge.ts` `fetchScorecardEntriesForCache`, mirroring the existing `__PERF_EMPLOYEE_SCORECARD__` on-demand-cache pattern) instead of the local maps. Verified live in the browser: added a note through the real "Add note" modal on `/performance-v22/scorecards` (Employee Scorecards tab) — toast "Review note saved", confirmed via direct API read-back that both the note and its auto-generated history entry persisted with the correct employeeId/period, and confirmed the note plus history line render back on the page after the modal closes (a genuine round trip, not an optimistic local update). Evidence upload and publish verified via direct multipart/JSON API calls (native file-picker dialogs aren't drivable through the browser automation used this session) — both 201, evidence returned a real `fileUrl`. All test data deleted afterward. Scope note deliberately left undone: `historyAdd()`'s other call sites (automatic "scorecard refreshed from review" bookkeeping, and the separate "update KPI actual/target value" flow — `saveUpdate`/`saveRecord`, `V.updates`) still write to the local `V.history`/`V.updates` maps only; the backend already appends its own history entry for every note/evidence/publish write, so the real history list populates without touching those call sites, and the KPI-update flow has its own already-real endpoint (`PATCH /performance/goals/:id/progress`) that a future pass can wire using the per-row `goal.id` `rowsFor()` already carries. **Applied to VPS `arcus_dev` on 2026-09-09** via `docker exec arcus-dev-api-1 npm run db:migrate:performance-scorecard-entries` immediately after the staff+API deploy: `[ok] created performance_scorecard_entries`. Verified live on the deployed API — `GET /api/performance/scorecard-entries?employeeId=…&period=…` returns 200 with the real grouped payload (401 unauthenticated), so table + service + controller + routes + permissions all line up on dev. |

| `2026-09-09-financial-reports-table-local` | Create `financial_reports` (+ its two indexes) on **local** `arcus_dev`, then run the two dependent migrations that had been failing | `mysql < scripts/sql/add_financial_reports_table.sql` (applied via a one-off Prisma `$executeRawUnsafe` runner), then `npm run db:migrate:financial-reports-report-type-varchar` and `npm run db:migrate:financial-reports-reporting-period` | `true` | `n/a` | Found by running a full `npm run db:migrate:all` sweep: it reported `[FAILED]` on both `financial-reports-*` migrations with "Table financial_reports does not exist." The `2026-09-07-financial-reports-table-prod-demo` row above applied this table to **nvccz_prod and arcus_prod only** — local `arcus_dev` was never given it, even though 18 source files reference it (FinancialReportService, ApplicantPortalController / investee portal). The table is deliberately managed outside Prisma, so neither `db push` nor any Prisma migration would ever have created it. After creating it, both dependent migrations succeeded (`reportType` widened to VARCHAR(64); `reportingPeriod` column + two indexes added) and a re-run of the full sweep completed with **zero `[FAILED]` lines**. `appliedToVps` is `n/a` because this is the local half of an already-applied VPS change, not a new delta. |

| `2026-09-09-full-migrate-all-verified-local` | Verification only: full `db:migrate:all` sweep on local `arcus_dev` | `npm run db:migrate:all` | `true` | `n/a` | Ran end to end twice — once to find drift, once to confirm the fix. The first pass also created objects that were genuinely missing locally (`reporting_notification_logs`, its performance index, and a `reporting_period_operational_kpis` FK), which is exactly the drift this sweep exists to catch. Second pass: zero failures. Note two migrations under `:all-dbs` naming legitimately no-op in a single-DB context, as recorded in the `2026-09-07-db-migrate-all-prod` row. |

| `2026-09-09-performance-role-permissions-vps` | Data-only: apply the `performance.*` / `accounting.timesheets.*` grants to the 52 internal roles on **VPS `arcus_dev`** | `docker exec arcus-dev-api-1 npm run db:migrate:performance-role-permissions` (idempotent) | `n/a` | `true` | **Caught during post-deploy verification, and it would have broken the module.** The `2026-09-08-performance-endpoint-guards-pass2` work added `requireAnyPermission`/`methodGuard` to 118 write endpoints and their reads, but its permission grants had only ever been applied locally (`appliedToVps=false`). Deploying that guard code to dev without the grants meant every guarded route returned **403 for every user** — confirmed by probing the freshly-deployed API as `admin@nts.com`: `/performance/goals`, `/performance/scorecards/board` and `/accounting/timesheets/mine` all 403. After running the grant migration (`granted=52 skipped=0 excluded=6`) the same probes return **200**. Lesson worth keeping: this pair is code+data coupled — never ship the guards to an environment without running the grants there in the same window. |

| `2026-09-09-prod-demo-deliberately-not-migrated` | **Nothing applied.** State check only of `nvccz_prod` and `arcus_prod` (demo) | n/a — see notes for the correct sequence | `n/a` | `false` | Checked whether the Performance work should be pushed to the two client databases and concluded **not yet**, on evidence rather than caution: (1) neither API container has `src/middleware/methodGuard.ts` (`GUARD_ABSENT` on both), so neither runs the guarded code; (2) neither image contains `run-performance-scorecard-entries-migration.ts` or `run-performance-role-permissions-migration.ts`, so the migrations cannot be invoked there anyway; (3) both scripts call `assertDevDatabase`, which refuses any database not named `nts`/`arcus_dev` unless `UAT_ALLOW_NON_DEV_DB=1` — a deliberate rail that would have to be bypassed. Measured `nvccz_prod`: `performance_scorecard_entries` absent, and **0 of 57 roles** hold `performance.goals.manage` — i.e. old code + old data, internally consistent and not broken. Applying the migrations now would add an unused table and grant 52 production roles broad `performance.*` permissions that nothing would check, while risking a visible change if anything reads `role.permissions` for UI visibility. **Correct sequence when prod does ship:** deploy the API code first, then in the same window run `db:migrate:performance-scorecard-entries` **and** `db:migrate:performance-role-permissions` — the guards and their grants are code+data coupled, and shipping the guards without the grants returns 403 to every user on every guarded route (observed on dev this same day, see the row above). |

## How to add a new entry

When you run a migration locally, append a row:

```md
| `YYYY-MM-DD-short-slug` | What changed | Exact SQL or npm script | `true` | `false` | Context |
```

## Apply-to-VPS checklist (when asked)

1. SSH / tunnel to Contabo MySQL.
2. For each row with `appliedToVps=false` that is safe for Contabo, run `how to apply`.
3. Flip `appliedToVps` to `true` and note the date in `notes`.
